apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-bank-swagger-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  ingressClassName: higress
  rules:
    - host: "swagger.lookeke.com"
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: swagger-simple-bank-api
                port:
                  number: 80
  tls:
    - hosts:
        - swagger.lookeke.com
      secretName: simple-bank-swagger-cert
