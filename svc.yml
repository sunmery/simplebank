apiVersion: v1
kind: Service
metadata:
  name: prod-web-simple-bank-service
spec:
  selector:
    app: prod-simple-bank-web
  ports:
    - name: https
      port: 4430
      targetPort: 4430
      protocol: TCP
      nodePort: 30003
    - name: quic
      port: 4430
      targetPort: 4430
      protocol: UDP
      nodePort: 30004
  type: NodePort

